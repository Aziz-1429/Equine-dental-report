# Supabase project credentials (optional).
# Without these, EquiDentum still works fully offline — reports are
# saved to the browser's local storage instead of the cloud database,
# and the "Reports" dashboard will show a connection error.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
